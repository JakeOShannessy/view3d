version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Build lib
          command: make lib
      - run:
          name: Build binaries
          command: make
