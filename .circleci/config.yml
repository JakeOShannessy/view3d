version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Setup
          command: apt-get install gcc make -y
      - run:
          name: Build lib
          command: make lib
      - run:
          name: Build binaries
          command: make
